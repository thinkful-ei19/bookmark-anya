/* global store, api */

const bookmarkList = (fucntion() {
    const generateNewItems() {
        let description = ''
        if (!store.item.expanded) {
            description = `some html`
        }

        return
        
    }
}());